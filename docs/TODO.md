# TODO / 待办 · 源自 2026-08-10 深度审计报告

> 状态图例:🔴 未开始 · 🟡 进行中 · 🟢 已完成 · ⚪ 决策/观察中(未到动手条件)
> 优先级:P1 最高 → P3 低。改动交易逻辑前务必先看 `ROLLOUT.md` 的阶段与门槛。

## 已完成

| ID | 项目 | 状态 | 备注 |
|---|---|---|---|
| T0 | `trigger.veto` 量能埋点(记 recent_vol/need/ratio/why/would_entry) | 🟢 | 2026-08-10 上线;当天验证 LOFF 1.34× / SPCH 0.49× 均 `why=ok`,真低量非数据延迟,过滤器正确避开两笔亏损 |
| T9a | 每日 Ross 对比工作流(`ross_compare.py`) | 🟢 | 浏览器抓字幕→对比 watchlist;8/10 重叠=NAMI,8/11 重叠=STKH+JWEL(JWEL 是 Ross 当天最赚的 +$61k) |
| T11 | 匹配容忍字幕拼写变形(Jwell=JWEL、Zjy L=ZJYL) | 🟢 | `match_symbol` 加 exact/fuzzy 两级,防误配(NAMI 不进 dyNAMIc);6 单测通过 |
| T12 | 过滤器机会成本 + 入场方式对比(`filter_costs.py`/`entry_compare.py`) | 🟢 | 用真 RH 数据:①三道门整体**净保护**(躲 $1686 > 错过 $1368);量能门明确有效(净省 $296)②突破入场稳跑赢回踩(全池 +3.16R vs −7.82R)—— **回踩机制上漏 runner** |
| T13 | 加 3 个新影子变体(volx2-nochase / volx2-1.5x / micro-dip) | 🟢 | 2026-08-12;sim 7 场景 PASS;修入场机制而非选股 |
| T15 | 回测新变体做大样本粗筛(扩展 `backtest.py`) | 🟢 | 400 跳空日:**volx2-1.5x 胜出**(47笔/77%/+22.3R,比 2× 多交易且总R更高);**micro-dip 否决**(73笔/16%/−0.83R,紧止损被噪声扫穿——又一个小样本运气);nochase 回测测不了(=volx2) |
| T5 | 排查 `hod-dip` 一个月 0 报警 | 🟢 | 2026-08-19,源自 Ross 对比:8/18 视频里他的动能票(GNPX 7:05am、SXTC 8:30am)都在我们 9:40 窗口打开前就走完了,且"+3%/5min"是瞬时条件,5min 轮询容易跨过去没采到样;改为 9:32 起、90s 轮询,并加 `hod.scan` 埋点(每次轮询都记 hit 数,不再是纯沉默)。**待观察**:改完后是否还是 0 |

## P0 · 紧急,下次盘前时段应先测

| ID | 项目 | 为什么 | 涉及 | 状态 |
|---|---|---|---|---|
| T18 | Robinhood 主雷达 gap 扫描(`scan_tickers`/`SCAN_ID`)自埋点以来(7/16 起,26 次)**贡献 0 只票,一次没有** | 2026-08-20 Ross 对比时顺手查的,2026-08-21 盘前实测排除了"盘前时段算不进去"的猜测:9:08 ET(盘前)`total_items=0`,9:33 ET(开盘 3 分钟后)**依然 0**。同一时刻我们自己的 Alpaca+IEX 扫描抓到了 BIVI(gap 56.1%、rvol 26.8、float 805万、价 2.06)—— **完全符合 RH 那个 scan 自己声明的四条过滤条件**(gap>10%/价2-20/rvol>5/float<20M),但 RH scan 还是 0。排除了"没有票达标"和"盘前数据算不进去"两种解释,说明 `SCAN_ID` 这个已保存的扫描本身可能是坏的/过期的/账号关联错了,不是时间点问题。**下一步**(未动手,等你过目):建 RH APP 里重新建一个同样条件的新 scan、拿新 scan_id 替换,或者直接找 RH 官方支持排查这个 scan_id | `rh.py scan_tickers()` `scanner.py get_universe()` | 🔴 根因缩小了,方案待定 |
| — | 2026-08-25 用 `update_scan_filters`(原样过滤器覆盖式刷新,内容不变)试过一次"戳一下"的低风险修复,你同意后做的 | 2026-08-26 排的两次盘前验证(9:08/9:33 ET)因为 SSH 连接问题没跑成(空结果/连接中断),没拿到刷新后的真实数据 | — | 🔴 已重排 8/28 盘前重测 |

## P1 · 最高优先

| ID | 项目 | 为什么 | 涉及 | 状态 |
|---|---|---|---|---|
| T1 | ~~把 `orb5-dip` 转实盘~~ | +$93 是 **9 笔小样本运气**;更大回放(T12)里回踩全池 −7.82R、机制上漏 runner,且止损并不比突破紧 | — | ❌ **否决**,不转正 |
| T14 | 多账户 paper:先上 **volx2-1.5x**(回测冠军)+ **volx2-nochase**(回测盲区,须真实成交验) | 单账户多策略会持仓合并/抢仓/熔断连坐;真实滑点只有 paper 能暴露(WLDS 的 $0.04) | `executor`/`autotrader` 读多套 key | 🟢 2026-08-12 上线,三账户独立跑至今;**样本还远不到 T15 定的 ≥30 笔晋升阈值**(现役 13 笔、1.5x 3 笔、nochase 2 笔),继续攒 |
| T16 | micro-dip:回测否决后从影子里退役(或留作反面对照) | 大样本 16% 胜率,继续占对照位意义不大 | `autotrader.py SHADOW_VARIANTS` | ⚪ 可选 |

## P2 · 中优先

| ID | 项目 | 为什么 | 涉及 | 状态 |
|---|---|---|---|---|
| T2 | 修实盘入场的"延迟劣势":封顶追高幅度 | 证据攒够了才动手,不是猜:WFF(8/18)追到 2.79,1.5x 账户同票同天 2.50 进(+$60 vs -$37);NBIZ(8/20)同样输给 nochase 的箱顶限价。2026-08-21 上线:确认价超过箱顶+0.5×箱体范围就否决,不追(跟 hod_poll 现成的 0.5R 逻辑一致)。回测历史 7 笔实盘:只会否掉 WFF 这一笔,NBIZ($6.98 vs 封顶 $6.995,差一点没够)、WLDS(单纯 $0.04 滑点不算追高)、BOXL(正常 -1R 止损)、KOS(唯一赢的)都不受影响 | `autotrader.py` `CHASE_MAX_FRAC` | 🟢 已上线,观察后续实盘样本 |
| T3 | 加**新闻催化剂**过滤 | Ross 要求有催化剂;现在只显示不筛 | `scanner.py` `latest_headline()` | 🔴 |
| T4 | 加**流动性检查**:下单量 < 1分钟量的 2–5% | 薄票大单滑点大。2026-08-27 上线:`executor.buy()` 加 `recent_vol` 参数(复用 autotrader 已测的突破分钟量),超过 3% 就缩小仓位不拒单。回测 WFF/NBIZ/BOXL:突破分钟量几万到十几万股 vs 我们 200-400 股仓位,从没被这条碰到过,纯安全兜底 | `scanner.py` / `executor.py buy()` | 🟢 已上线(TG 确认后在会话里改口头批准合并) |

## P3 · 低优先 / 排查类

| ID | 项目 | 为什么 | 涉及 | 状态 |
|---|---|---|---|---|
| T9 | 加「衰竭/背面」过滤,别追已暴涨完的票 | Ross 明确回避暴涨后的"背面";2026-08-10 我们把 NAMI 放回 watchlist,它开盘即熄火(OR高 3.525 ≪ 盘前 4.29)。2026-08-27 上线:9:15扫描时价格比"当天至今最高价(含盘前)"回落超15%就拒绝进候选池。⚠️ 跟 T2 不同,这条没法回测证明——NAMI 只证明这个模式真实发生过,证不了这条规则整体赚不赚钱 | `scanner.py FADE_MAX_FRAC` | 🟢 已上线,观察 |
| T10 | ~~查扫描器为何漏掉 ZJYL~~ | 8/11 视频里 Ross 自己也**因 float 太高跳过 ZJYL** —— 我们的 float 过滤方向是对的,漏掉它是对的 | — | ⚪ 降级/关闭:验证了 float 过滤 |
| T6 | 收紧止损上限(0.50 → 更紧,靠拢 Ross ≤15¢) | 宽止损→薄票上仓位偏大 | `autotrader.py STOP_DIST_MAX` | ⚪ 需回测验证影响 |
| T7 | 降 `account.anomaly` 频率(Alpaca 偶返 last_equity=0) | 一天 78 条,虽有本地兜底但偏频 | `executor.py daily_pnl()` | ⚪ 观察 |
| T8 | 流通盘上限 20M → 收紧靠拢 Ross <10M | 现在会放进中盘 | `scanner.py FLOAT_MAX` | ⚪ 需回测验证 |
| T17 | 盘前扫描只跑一次(9:15 ET),抓不到纯盘中启动的动能票 | 8/18 对比:Ross 的 GNPX(7:05am)、SXTC(8:30am)都在开盘前就拉完了,不是"跳空股",我们的架构上就看不到 —— 这比 hod-dip(T5,9:30 后的窗口)更早、更根本,是独立的第三雷达需求 | `scanner.py`(需盘前多次轮询或换 RH 实时扫描) | ⚪ 需求明确,方案未定;上线前必须先过 `ROLLOUT.md` 门槛(新雷达 = 新的下单来源) |

## 明确不动(健康,别碰)

- `executor.py` 熔断(−$300):07-15 正确触发、止住更大亏损
- 下单执行链路:买入价分毫不差、无卡单/裸仓、止损按计划触发
- bracket 单机制、单实例锁、11:00 清仓
